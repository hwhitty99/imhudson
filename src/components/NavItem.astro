---
const { page, href } = Astro.props;
---

<li role="menuitem">
    <a
        href={href}
        x-ref={page}
        @mouseover=`hoveredNavItem = '${page}'`
        @mouseleave="hoveredNavItem = activePage"
        class="block rounded-xl px-4 py-2 capitalize transition-colors"
        :class=`{ 'max-md:bg-white max-md:text-black': isActive('${page}'), 'text-black': isActive('${page}') && hoveredNavItem == '${page}', 'md:hover:text-black': !isActive('${page}') }`
        :aria-current=`isActive('${page}') ? 'page' : 'false'`
    >
        {page}
    </a>
</li>
